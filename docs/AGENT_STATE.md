# OCObot — Agent State (Persistent Continuity)

_Last updated: 2026-09-15 — Postman Agent session (reconciled to real state: R1 complete; R2 rollover-rehydration now UNIT-VERIFIED (dedicated tests added) — no longer unit-tests-pending; full offline suite GREEN (59 passed). Working tree still carries uncommitted R1+R2 changes across 4 tracked files; tests/test_apply_monitor_result_r2.py is untracked)._

Maintenance rule: update this file when a MAJOR fact changes (subsystem completed, architecture decision, important test pass/fail, new blocker, stopping point moves). Keep it concise. Do not rewrite fully after minor actions.

## 0. How to use this file (read first, do not full-rescan)
1. Read this file.
2. `git status` and `git log --oneline -10`.
3. Inspect only the files relevant to the current stopping point / your task.
4. Verify only what is necessary; continue from the actual state.
- The LOCAL working tree is the source of truth. Never clone/reset/pull older origin over local work, and never discard uncommitted changes.

## A. Current state
- Project: OCObot — Binance Spot OCO management. Scope = safe monitoring, editing and replacement of ONE selected OCO, identified by `orderListId` (never symbol alone).
- Operating progression: PAPER/DEMO -> TESTNET -> LIVE. LIVE is intentionally DISABLED (BinanceOCOProvider raises if constructed with mode=LIVE).
- Layers: `domain` (models, validation) | `application` (monitor_coordinator, dynamic_monitor, monitor_engine, live_price, dynamic_stop, services; legacy `auto_trail.py` still on disk but UNREFERENCED by the main UI) | `providers` (binance, paper, demo_monitor, sample_data, base) | `ui` (main_window with async Dynamic Monitor + DynamicMonitorPanel; plus dashboard/testnet/stage12 modules).
- Active UI path: canonical asynchronous Dynamic Monitor (has replaced the legacy AutoTrail path).
- Safety constraints in force: exact `orderListId` identity; all operations isolated to the selected OCO; explicit replacement failure states; NO blind retry; never search/modify another OCO as a recovery shortcut; LIVE disabled until safety evidence + human review.

## B. Verified facts
- [VERIFIED 2026-09-15] Full offline suite GREEN — **59 passed** via `python -m pytest --ignore=tests/test_testnet_acceptance.py -p no:cacheprovider` with BOTH R1 (stop token) AND R2 (rollover rehydration) present in the working tree, including the R2 dedicated tests now collected (up from the previously documented 55) ⇒ R2 did NOT regress the suite.
- [VERIFIED 2026-09-15] R1 atomic invariant preserved: the stop token is NEVER checked between `cancel_oco` and `place_oco`. R1 unit tests confirm: stop-before-cancel ⇒ no mutation / state `ABORTED_NO_CREATE`; stop-after-cancel ⇒ create still completes to `SUCCESS` (id rolls forward).
- [VERIFIED 2026-09-15] R2 at the unit level — `tests/test_apply_monitor_result_r2.py` (4 tests) covers: success rehydrates to the new `orderListId` (2003); re-select failure is fail-safe (clears selection/original/draft, surfaces NEEDS_REFRESH via `set_monitoring_state(False, ...)`, never auto-selects another OCO); the NEW non-success-no-rollover test — a non-success result (`ABORTED_NO_CREATE`) does NOT rehydrate (`select()` not called, selection unchanged, `monitor_coordinator=None`, `set_monitoring_state(False, ...)`); and the stale-generation guard test drops late results.
- [NOT VERIFIED] Testnet Gate 1 has NOT been executed against real Testnet credentials this session.
- [NOT VERIFIED] Historical replacement latency (~2852.79 ms Testnet, target TUTUSDT old `orderListId` 98130 -> new 98135; control NOTUSDT 97902 unchanged) has NOT been re-verified this session. [REPORTED, historical only] — NOT a production latency guarantee.
- [REPORTED] Binance payload fix: unsupported `aboveTimeInForce` for `LIMIT_MAKER` identified and fixed (confirmed by unit test test_binance_testnet_setup.py).

## C. Recent work
- **NEW (this session) — cooperative stop token in `src/ocobot/application/monitor_coordinator.py`** (closes review findings #1 and #2, the async-lifecycle race): a `threading.Event` on `DynamicMonitorCoordinator`. `__init__` creates `self._stop_event`; `start()` clears it; `stop()` SETS it WITHOUT acquiring the lock (so it can interrupt an in-flight `_replace` blocked on a provider call). `_replace` checks the token (a) at entry before get_oco/validation and (b) immediately before `cancel_oco`, aborting via `_abort_for_stop()` — returns state `ABORTED_NO_CREATE`, resets engine `replacement_in_flight`/`trigger_latched`, does NOT set `_failed` (a clean no-op stop, not a failure). CRITICAL INVARIANT (documented in code): the token is NEVER checked between `cancel_oco` and `place_oco` — once cancel is issued the cancel->create pair is ATOMIC and must complete, so the exchange is never left with a cancelled-but-unreplaced OCO. `on_price` returns early on a non-success `_replace` result before any rollover/engine advancement. R1 is COMPLETE: it now has dedicated unit tests in `tests/test_monitor_coordinator.py` (stop-before-cancel ⇒ no mutation / `ABORTED_NO_CREATE`; stop-after-cancel ⇒ create still completes to `SUCCESS`).
- **NEW (this session) — R2 rollover rehydration in `src/ocobot/ui/main_window.py` `_apply_monitor_result`** (code-complete AND unit-verified): on a successful auto-rollover the UI now re-selects the NEW `orderListId` via `OCOEditorService.select()`. DESIGN: on re-select FAILURE, clear the stale selection (`selection`/`original`/`draft = None` + `_clear_selection_ui()`), surface a `NEEDS_REFRESH` message (via `_trail_event` + `set_monitoring_state(False, ...)`), and NEVER auto-select another OCO. The generation guard stays upstream in `_finish_monitor_price`. The atomic cancel→create sequence is unchanged. UNIT-VERIFIED by `tests/test_apply_monitor_result_r2.py` (untracked, 4 tests: success-rehydrate, reselect-failure-failsafe, the NEW non-success-no-rollover test, and the stale-generation guard test; suite 59 passed).
- `src/ocobot/ui/main_window.py` (committed): async Dynamic Monitor wired in — `_start_dynamic_monitor`, `_finish_monitor_start`, `_queue_monitor_price`, `_finish_monitor_price`, `_apply_monitor_result`, `_stop_dynamic_monitor`; generation guards; price coalescing; single-worker ThreadPoolExecutor; rollover to new `orderListId`; monitor shutdown in `closeEvent`. Legacy AutoTrail UI removed.
- `src/ocobot/application/monitor_coordinator.py` (base, committed): result states `SUCCESS` / `ABORTED_NO_CREATE` / `FAILED_NEEDS_ATTENTION`; explicit pre-cancel vs post-cancel failure distinction; sequence cancel -> get price -> calc levels -> build payload -> place_oco -> verify new `orderListId`, with cancelled-exception tagging.
- `tests/test_monitor_coordinator.py` (committed): FakeProvider unit tests for FAILED_NEEDS_ATTENTION (no retry), ABORTED_NO_CREATE (pre-cancel), and successful rollover (cancelled==[100,200]).
- `tests/test_testnet_acceptance.py` (committed): opt-in, never-LIVE automated Testnet acceptance test for release Gate 1. Exercises the real `DynamicMonitorCoordinator` path end-to-end on Testnet: ensures a target + a control disposable OCO, forces a reposition, and asserts only the selected OCO is replaced (new orderListId) while the control OCO remains open and the old id is gone; cancels the disposable orders in cleanup. Guarded by `pytest.mark.skipif` requiring `BINANCE_TESTNET_API_KEY` + `BINANCE_TESTNET_API_SECRET` + `OCOBOT_RUN_TESTNET_ACCEPTANCE=1`, so it SKIPS in normal runs.
- Docs: README.md, docs/PROJECT_ROADMAP.md, docs/TESTNET.md updated; docs/RELEASE_CHECKLIST.md present.

## D. Current stopping point
- Git: branch `main`, HEAD `226b469 Enhance dynamic monitoring and testnet acceptance`, `main == origin/main`. The Dynamic Monitor feature and the Testnet acceptance test are COMMITTED at HEAD. Nothing is staged or committed this session.
- Working tree has FOUR uncommitted TRACKED changes this session (this CORRECTS the earlier note that claimed only 1 uncommitted file): `docs/AGENT_STATE.md`, `src/ocobot/application/monitor_coordinator.py` (R1 stop token), `tests/test_monitor_coordinator.py` (R1 tests), and `src/ocobot/ui/main_window.py` (R2 rollover rehydration). Nothing is staged. Additionally `tests/test_apply_monitor_result_r2.py` (the R2 unit tests, 4 tests) is UNTRACKED.
- Scratch-log cleanup this session: several root scratch pytest logs (`_r1.txt`, `_r2_probe.txt`, `_r2_pytest.txt`, `_tmp_out.txt`, `_tmp_pytest.txt`, `_tmp_pytest_full.txt`) were removed as noise. Remaining untracked noise at repo root — `_diff_mc.txt`, `_diff_r1_out.txt`, `_r1v.txt`, `_tmp_pytest_run.txt`, `nultype`, `out_baseline.txt` — is NOT part of the work.
- Finished: Dynamic Monitor implementation + unit tests + doc updates (committed); legacy AutoTrail removed from UI; guarded Testnet acceptance test authored + committed; R1 cooperative stop token + R1 unit tests applied this session (uncommitted); R2 rollover rehydration code applied AND unit-verified this session via `tests/test_apply_monitor_result_r2.py` (untracked, 4 tests); R3 symbol guard IN PROGRESS; full offline suite = 59 passed.
- Remaining / release gates:
  - R2 dedicated unit tests (`tests/test_apply_monitor_result_r2.py`, untracked) — DONE (4 tests: success-rehydrate, reselect-failure-failsafe, the NEW non-success-no-rollover test, and the stale-generation guard test; suite 59 passed).
  - Gate 1 (Testnet acceptance): committed but NOT yet executed against real Testnet credentials. To run it, see section F.
  - Gate 2 [BLOCKED — needs human]: safety/code review.
  - LIVE stays disabled until both gates complete and release is explicitly approved.

### Review findings (2026-09-15) & remaining work
Prioritized architectural review of the Dynamic Monitor + rollover.

- **CONFIRMED SAFE (do not re-fix):** exact `orderListId` scoping everywhere (cancel re-fetches + identity-asserts, never symbol-only); no blind retry after `FAILED_NEEDS_ATTENTION`; correct pre-cancel (`ABORTED_NO_CREATE`) vs post-cancel (`FAILED_NEEDS_ATTENTION`) distinction; LIVE disabled; engine/coordinator overlap guard (lock + `replacement_in_flight` + single-worker executor); reference/trigger reset after success; coordinator rolls target id forward on success.
- **ADDRESSED this session:** Finding #1 (in-flight replacement still mutating Binance after Stop) and Finding #2 (same race on OCO-change / mode-switch) — root cause fixed by the cooperative stop token above (`stop()` is already called on Stop and on selection/mode change). Finding #4 (rollover rehydration, R2) — code-complete in `_apply_monitor_result` (see section C / R2 below) AND unit-verified (`tests/test_apply_monitor_result_r2.py`, 4 tests; suite 59 passed). Finding #5 (R3 symbol guard) is IN PROGRESS this session (see R3 below).
- **REMAINING WORK (prioritized, next session):**
  - **(R1) [DONE]** Stop-token unit tests ADDED to `tests/test_monitor_coordinator.py`: "stop before cancel" (`cancel_oco`/`place_oco` never called, `_failed` stays False, state `ABORTED_NO_CREATE`) and "atomicity: stop after cancel still completes create" (`place_oco` still called, `order_list_id` rolls forward, state `SUCCESS`). Verified in the full suite (59 passed).
  - **(R2) [DONE — code-complete AND unit-verified]** Finding #4 — rollover rehydration: `ui/main_window.py` `_apply_monitor_result` success path now re-selects the NEW `orderListId` via `self.service.select(new_id)` (in try/except). On re-select FAILURE it clears the stale selection (`selection`/`original`/`draft = None` + `_clear_selection_ui()`), surfaces a `NEEDS_REFRESH` message (`_trail_event` + `set_monitoring_state(False, ...)`), and NEVER auto-selects another OCO — WITHOUT bumping the monitor generation (the generation guard stays upstream in `_finish_monitor_price`). UNIT-VERIFIED: `tests/test_apply_monitor_result_r2.py` (untracked) has 4 tests — (1) success-rehydrate to new id (2003); (2) reselect-failure-failsafe; (3) the NEW non-success-no-rollover test — an `ABORTED_NO_CREATE` result does NOT rehydrate (`select()` not called, selection unchanged, `monitor_coordinator=None`, `set_monitoring_state(False, ...)`); (4) the stale-generation guard test. Full suite re-run GREEN (59 passed).
  - **(R3) [IN PROGRESS this session]** Finding #5 — symbol guard (currently being implemented this session): in `_queue_monitor_price`/`_on_price`, drop price snapshots whose symbol != the currently selected/monitored symbol (prevents a stale snapshot feeding a freshly-created coordinator).
  - **(R4)** Finding #3 — provider-close race: in `_switch_mode` PAPER branch + testnet connect branch, add a BOUNDED best-effort drain of the in-flight monitor future (e.g. `self._monitor_future.result(timeout=2)` in try/except) before `old.close()`, so an in-flight signed request finishes before the old provider's HTTP client is closed. Must never hang the UI.
  - **(R5)** Finding #6 (note only, not a bug): `_load_order_list` issues N+1 signed requests per OCO — potential latency optimization, not a correctness issue.
  - **(R6)** Gate 1 (Testnet acceptance) still NOT executed against real Testnet creds; Gate 2 (human safety/code review) still pending; LIVE stays disabled.
- **TOOLING NOTE (for future sessions):** Windows cmd.exe has NO `sed` and NO heredoc, and it BACKGROUNDS long-running output ⇒ write/edit files with the editor tool (never shell heredoc/echo/cat), and run pytest with output REDIRECTED to a file (`... > out.txt 2>&1`) then read that file.

## E. Development direction
- NOTE: the replacement result states in `monitor_coordinator.py` (`SUCCESS` / `ABORTED_NO_CREATE` / `FAILED_NEEDS_ATTENTION`) are module-level STRING constants, NOT a `ReplacementState` enum.
- Roadmap phases 0-12 largely implemented (freeze spec, price engine, OCO reader, base/stage-2 UI, home, orders/manual edit, dynamic monitoring calc, monitor engine, safe replacement, monitoring UI, engine tests, paper/demo, testnet). Phase 13 (safety/timing review) pending; Phase 14 (LIVE) not started, gated.
- Dynamic Monitor config inputs: Reposition Trigger Rise %, TP Distance Above Current %, SL Distance Below Current %. Calculations: `TP = live*(1+TP%/100)`; `SL Trigger = live*(1-SL%/100)`; `SL Limit = normalized SL Trigger + 1*tickSize`. Invariant: `SL Trigger < SL Limit < live`. Validation rejects non-finite, non-positive trigger/TP/SL, and SL >= 100.

## F. Next-session context
- Start here (see section 0). Do NOT re-scan the whole repo.
- Most relevant modules: `src/ocobot/application/monitor_coordinator.py`, `src/ocobot/ui/main_window.py`, `src/ocobot/ui/dynamic_monitor_panel.py`, `src/ocobot/application/dynamic_monitor.py`, `tests/test_monitor_coordinator.py`, `tests/test_testnet_acceptance.py`.
- To execute the Testnet acceptance gate (Gate 1): set `BINANCE_TESTNET_API_KEY`, `BINANCE_TESTNET_API_SECRET`, and `OCOBOT_RUN_TESTNET_ACCEPTANCE=1` (optional overrides: `OCOBOT_TESTNET_TARGET`, `OCOBOT_TESTNET_CONTROL`, `OCOBOT_TESTNET_QUOTE`), then run `python -m pytest -q tests/test_testnet_acceptance.py`. It places small disposable Testnet orders and cancels them afterwards. Never LIVE.
- Offline full-suite run this session: `python -m pytest --ignore=tests/test_testnet_acceptance.py -p no:cacheprovider` ⇒ 59 passed.
- The uncommitted R1+R2 changes across the 4 tracked files (docs/AGENT_STATE.md, monitor_coordinator.py, tests/test_monitor_coordinator.py, ui/main_window.py) plus the untracked `tests/test_apply_monitor_result_r2.py` (the R2 unit tests) are valuable — preserve them. Human operator can commit/push later via terminal.
- Do NOT enable LIVE. Do NOT modify other OCOs as a recovery shortcut. Keep the project OCO-focused.
- Candidate next work (priorities, not hard constraints): (1) R2 unit tests in `tests/test_apply_monitor_result_r2.py` — DONE (4 tests, offline full-suite ⇒ 59 passed); current focus (2) R3 symbol guard on price snapshots — IN PROGRESS; then (3) R4 drain in-flight monitor future before `provider.close()` on mode switch; (4) Gate 1 Testnet with dedicated creds; (5) Gate 2 human review. R5 later.
